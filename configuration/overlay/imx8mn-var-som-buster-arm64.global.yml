general:
    parameters:
        mender_device_type: imx8mn-var-som

bootstrap:
    architecture: arm64

playbooks:
    300_board_support:
        parameters:
            ebs_template: imx8mn-var-som
            kernel_image: linux-image-4.14.98-gf54b007d1548
            uboot_device_offset: '0x400000'
            uboot_env_size: '0x1000'
    900_base_system_cleanup:
        parameters:
            hostname: imx8mn-var-som

shared_folders:
    edi_workspace:
        folder: edi-workspace
        mountpoint: edi-workspace
